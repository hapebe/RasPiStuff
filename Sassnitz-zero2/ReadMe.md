# Take pictures from a USB camera on a Pi Zero - and various complications arise

## Install

The scripts require quite a lot of software packages, the following list is probably incomplete:

```
sudo apt install jq